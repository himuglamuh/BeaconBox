# BeaconBox Boot Configuration
# For more options, see https://rptl.io/configtxt

[all]
# 64-bit mode
arm_64bit=1

# Enable UART
enable_uart=1

# GPU memory split (adjust based on your needs)
gpu_mem=64

# Video and display
dtoverlay=vc4-kms-v3d
max_framebuffers=2

# Audio
dtparam=audio=on

# I2C and SPI (enable if needed)
dtparam=i2c_arm=on
dtparam=spi=on

# Boost performance
arm_boost=1

# Common overlays for all models
dtoverlay=dwc2

[pi400]
# Pi 400 has onboard cooling, so can run slightly faster
arm_freq=1800

[pi4]
# Pi 4 specific settings
kernel=kernel8.img
# Enable USB boot
otg_mode=1
# 64-bit kernels for Pi 4
arm_64bit=1

[pi5]
# Pi 5 specific settings
kernel=kernel_2712.img
# Pi 5 has different HDMI setup
dtoverlay=vc4-kms-v3d-pi5
# Pi 5 specific firmware and bootloader
arm_64bit=1
# Ensure proper USB and PCIe functionality
dtparam=pciex1_gen=3

[cm4]
# Compute Module 4 settings
otg_mode=1

[pi02]
# Pi Zero 2W specific
kernel=kernel8.img
arm_64bit=1

[pi0]
# Pi Zero (original) - 32-bit only
kernel=kernel.img
arm_64bit=0

[all]
# Additional hardware support
dtoverlay=gpio-poweroff
dtoverlay=gpio-shutdown

# Network boot options (uncomment if needed)
# program_usb_boot_mode=1
